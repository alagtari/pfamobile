import 'dart:ui';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:mobile/theme/colors.dart';
import 'package:mobile/theme/doubles.dart';

class CustomRaius {
  CustomRaius._();

  static const extraExtraSmallRadius = Radius.circular(
    extraExtraSmallRadiusSize,
  );
  static const extraSmallRadius = Radius.circular(
    extraSmallRadiusSize,
  );
  static const smallRadius = Radius.circular(
    smallRadiusSize,
  );
  static const mediumRadius = Radius.circular(
    mediumRadiusSize,
  );
  static const largeRadius = Radius.circular(
    largeRadiusSize,
  );
  static const extraLargeRadius = Radius.circular(
    extraLargeRadiusSize,
  );
  static const extraExtraLargeRadius = Radius.circular(
    extraExtraLargeRadiusSize,
  );
}
